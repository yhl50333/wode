#server
server:
  port: 80
  servlet:
    context-path: /
#HikariCp
spring:
  datasource:
    url: jdbc:mysql:///dbactivity?serverTimezone=GMT%2B8&characterEncoding=utf8
    username: root
    password: root
  thymeleaf: #thymeleaf
    prefix: classpath:/templates/pages/
#mybatis
mybatis:
  mapper-locations:
    - classpath:/mapper/*/*.xml
#log
logging:
  level:
    com.cy: debug

